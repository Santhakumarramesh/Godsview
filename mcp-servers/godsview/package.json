{
  "name": "@godsview/mcp",
  "version": "1.3.0",
  "description": "GodsView MCP server v1.3 — read-only inspection. Adds get_macro_risk (FRED + macro_engine + news lockout aggregator) on top of v1.2's 15 tools. No order placement, no chart control, no live writes.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "godsview-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "tsx": "^4.19.0"
  }
}
