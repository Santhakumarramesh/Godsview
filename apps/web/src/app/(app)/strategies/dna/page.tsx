import { ToDoBanner } from "@/components/ToDoBanner";

export default function StrategiesDnaPage() {
  return (
    <ToDoBanner
      title="Strategies · DNA"
      phase="Phase 6"
      description="Strategy DNA fingerprint — the regimes, sessions, and symbols where each strategy thrives or fails, derived from rolling MAE/MFE and outcome distributions."
    />
  );
}
