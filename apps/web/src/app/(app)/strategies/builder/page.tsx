import { ToDoBanner } from "@/components/ToDoBanner";

export default function StrategiesBuilderPage() {
  return (
    <ToDoBanner
      title="Strategies · Builder"
      phase="Phase 5"
      description="Define entries, exits, and filters with a typed DSL backed by the Quant Lab Strategy Builder. Versioned, diff-able, and promotable to paper."
    />
  );
}
