import { ToDoBanner } from "@/components/ToDoBanner";

export default function PortfolioCorrelationPage() {
  return (
    <ToDoBanner
      title="Portfolio · Correlation"
      phase="Phase 10"
      description="Rolling correlation matrix across open positions and tracked symbols, flagging crowded-trade risk."
    />
  );
}
