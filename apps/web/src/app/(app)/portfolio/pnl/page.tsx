import { ToDoBanner } from "@/components/ToDoBanner";

export default function PortfolioPnlPage() {
  return (
    <ToDoBanner
      title="Portfolio · PnL"
      phase="Phase 10"
      description="Realized + unrealized PnL per account, strategy, and symbol with intraday equity curve."
    />
  );
}
