Phase 1 prompt — live price and dynamic candles hardening
Inspect the current Godsview repo and implement Phase 1: harden live market price and dynamic candle updates.
Goals:
verify and improve the current live candle flow
ensure the chart always reflects the latest live market price
make candle updates smoother and more reliable
prevent duplicate stream/poll conflicts
support symbol switching and timeframe switching cleanly
keep the current LiveCandleChart.tsx but refactor if needed
Requirements:
inspect current dashboard.tsx and LiveCandleChart.tsx
inspect the backend endpoints currently used for:
/api/alpaca/candles
/api/alpaca/stream
/api/alpaca/ticker
confirm whether SSE + polling is redundant or intentional
make the live price and live candle rendering robust
ensure cleanup of EventSource connections
prevent stale symbol/timeframe updates from mutating the current chart
improve state management for:
livePrice
liveCandle
streamStatus
tickCount
Deliverables:
updated live chart behavior
cleaner stream lifecycle
better fault tolerance and reconnect logic
explanation of what was changed and why
Do not add bookmap or reversal cloud in this phase.
Phase 2 prompt — timeframe countdown timers
Inspect the Godsview repo and implement Phase 2: timeframe countdown timers.
Goals:
add a live candle-close countdown timer for the currently selected timeframe
support:
1Min
5Min
15Min
1Hour
1Day
ensure timers are accurate and update every second
align timer boundaries with candle close logic
optionally expose secondary timers for other timeframes if the architecture supports it cleanly
Requirements:
create a reusable utility for candle boundary calculation
use UTC-consistent logic unless the repo already follows a different convention
show a clean timer in the chart toolbar
timer must automatically change when timeframe changes
timer should not drift
timer should be modular and reusable elsewhere in the app
Suggested modules:
lib/market/clock.ts
optional hook like useCandleCountdown.ts
Deliverables:
working countdown timer in the chart UI
clean reusable countdown logic
explanation and test instructions
Do not add order book or heatmap yet.
Phase 3 prompt — backend order book and market microstructure ingestion
Inspect the Godsview repo and implement Phase 3: order book and market microstructure backend services.
Goals:
add backend support for order book/depth ingestion
prepare data structures needed for:
bookmap heatmap
liquidity zones
reversal cloud
per-candle microstructure
keep this phase backend-first
Requirements:
inspect current backend architecture and add new services cleanly
create new modules for:
order book snapshot normalization
streaming depth updates
price-level aggregation
rolling liquidity map state
design a clean in-memory model first
expose API endpoints or stream endpoints for frontend consumption
Suggested outputs:
/api/orderbook/snapshot
/api/orderbook/stream
/api/market/microstructure
/api/market/liquidity-zones
Suggested modules:
lib/market/orderbook.ts
lib/market/liquidityMap.ts
lib/market/types.ts
Important:
if true order book data is not yet available from the current source, create the architecture with graceful fallbacks and clearly mark mocked vs real paths
do not fake full production readiness if the data source is limited
document any Alpaca limitations honestly
Deliverables:
backend service layer
API or SSE/WebSocket endpoints
typed data contracts
explanation of source limitations
Do not render heatmaps yet.
Phase 4 prompt — frontend heatmap overlay and bookmap visualization
Inspect the Godsview repo and implement Phase 4: frontend heatmap overlay and bookmap-style liquidity visualization.
Goals:
overlay a bookmap-style heatmap on top of the live candle chart
visualize liquidity by price level over time
preserve the current candlestick renderer
add the heatmap as a separate render layer, not a replacement
Requirements:
do not remove lightweight-charts
build a custom overlay using canvas or another efficient rendering layer
heatmap should:
align with chart time axis
align with price scale
update live as new market depth arrives
support opacity/intensity scaling
keep performance reasonable
modularize the overlay so it can be toggled on/off
Suggested components:
HeatmapOverlayCanvas.tsx
integration inside LiveCandleChart.tsx
UI requirements:
add toolbar toggle for heatmap visibility
add intensity legend if cleanly possible
maintain current dark theme and style
Deliverables:
dynamic heatmap overlay
integration with live chart
explanation of mapping between order book data and rendered cloud
Do not add reversal logic yet.
Phase 5 prompt — reversal cloud and heatmap reversal points
Inspect the Godsview repo and implement Phase 5: reversal cloud and heatmap reversal point detection.
Goals:
identify reversal zones from market microstructure
show reversal points as a cloud/zone overlay on the chart
base the logic on:
liquidity concentration
absorption
imbalance
sweep-and-reclaim conditions
other available microstructure signals from the repo
Requirements:
create a reusable reversal scoring engine
produce structured outputs like:
price zone
confidence
direction bias
expiry / validity window
render reversal zones as cloud overlays on the chart
cloud should be visually different from the liquidity heatmap
Suggested modules:
lib/market/reversalCloud.ts
lib/market/reversalScoring.ts
ReversalCloudOverlay.tsx
Deliverables:
reversal detection logic
frontend cloud overlay
configurable thresholds
explanation of how reversal points are detected
Do not store historical microstructure yet unless necessary for the implementation.
Phase 6 prompt — per-candle bookmap metadata and enriched candle model
Inspect the Godsview repo and implement Phase 6: enriched candle model with per-candle bookmap metadata.
Goals:
attach market microstructure metadata to each candle
enable replay/debug/training of candle-level order flow context
Requirements:
extend the current candle model to include fields like:
bid liquidity concentration
ask liquidity concentration
liquidity imbalance
absorption score
sweep flag
reversal cloud score
build a transformation pipeline from raw order book data to candle-level summaries
keep the model typed and reusable
if persistence fits the repo architecture, add database schema support
Suggested schema/entities:
candle_features
liquidity_zones
heatmap_frames
Deliverables:
enriched candle feature pipeline
typed candle model updates
optional persistence layer
explanation of how these features can be used later in signal generation or replay
Phase 7 prompt — replay mode and analysis tooling
Inspect the Godsview repo and implement Phase 7: replay mode for candles, heatmap, and reversal cloud.
Goals:
replay historical candles along with:
live-style candle progression
heatmap overlay progression
reversal cloud progression
use replay to debug and validate the new visual/microstructure stack
Requirements:
create a replay controller
allow play/pause/speed controls
step candles forward in sequence
ensure heatmap and reversal cloud state evolve with replay
keep replay separate from live mode
make this a tool for debugging and later ML validation
Suggested components:
ReplayControls.tsx
useReplayEngine.ts
backend endpoint or local data loader for historical replay frames
Deliverables:
replayable chart mode
synchronized overlays during replay
explanation of how to use replay for debugging and future model training
Phase 8 prompt — polish, performance, and production readiness
Inspect the Godsview repo and implement Phase 8: polish, performance optimization, and production readiness.
Goals:
reduce rendering overhead
improve state flow and component boundaries
ensure overlays do not cause lag
improve reconnection handling and resilience
add feature toggles for advanced visual layers
Requirements:
profile likely performance hotspots
reduce unnecessary rerenders
isolate chart rendering from broader dashboard state
add config flags or UI toggles for:
heatmap
reversal cloud
timers
bookmap metadata panels
improve developer ergonomics and documentation
Deliverables:
cleaner architecture
performance-focused refinements
updated docs for the new live visualization stack
local testing guidance